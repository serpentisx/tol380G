
/* THIS CLASS IS NOT USED */

// ==========
// BALL STUFF
// ==========

// BALL STUFF

/*var g_ripple = {
  cx : 500,
  cy : 500,
  radius : 0,
  maxRadius : 100,
  speed : 4,
  BASE_SPEED : 4,
  accleration : 0.4,
  exploding : false
}

g_ripple.update = function (du) {
  if (!g_ball.hasPower) {
    this.cx = g_ball.cx;
    this.cy = g_ball.cy;
  }
  else if (this.exploding || eatKey(g_paddle.EXPLODE_BALL) && g_ball.hasPower) {
    this.cx = g_ball.cx;
    this.cy = g_ball.cy;
    this.speed += this.accleration * du;
    this.radius += this.speed;
    this.exploding = true;
    //g_ball.hasPower = false;
  }
  if (this.radius > this.maxRadius) {
    this.radius = 0;
    this.speed = this.BASE_SPEED;
    this.exploding = false;

  }
};

g_ripple.render = function (ctx) {
  ctx.fillStyle= "rgba(214, 80, 56, 0.37)";
  fillCircle(ctx, this.cx, this.cy, this.radius);
  ctx.fillStyle= "black";
};*/
